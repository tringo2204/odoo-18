<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sht_hr_training_course_view_tree" model="ir.ui.view">
        <field name="name">sht.hr.training.course.list</field>
        <field name="model">sht.hr.training.course</field>
        <field name="arch" type="xml">
            <list string="Training Courses">
                <field name="name"/>
                <field name="code"/>
                <field name="category"/>
                <field name="duration_hours"/>
                <field name="provider"/>
                <field name="is_active"/>
            </list>
        </field>
    </record>

    <record id="sht_hr_training_course_view_form" model="ir.ui.view">
        <field name="name">sht.hr.training.course.form</field>
        <field name="model">sht.hr.training.course</field>
        <field name="arch" type="xml">
            <form string="Training Course">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                            <field name="category"/>
                            <field name="is_active"/>
                        </group>
                        <group>
                            <field name="duration_hours"/>
                            <field name="provider"/>
                        </group>
                    </group>
                    <field name="description" widget="html"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="sht_hr_training_course_view_search" model="ir.ui.view">
        <field name="name">sht.hr.training.course.search</field>
        <field name="model">sht.hr.training.course</field>
        <field name="arch" type="xml">
            <search string="Training Courses">
                <field name="name"/>
                <field name="code"/>
                <field name="provider"/>
                <separator/>
                <filter name="sht_hr_training_course_filter_internal" string="Internal" domain="[('category', '=', 'internal')]"/>
                <filter name="sht_hr_training_course_filter_external" string="External" domain="[('category', '=', 'external')]"/>
                <filter name="sht_hr_training_course_filter_online" string="Online" domain="[('category', '=', 'online')]"/>
                <separator/>
                <filter name="sht_hr_training_course_filter_active" string="Active" domain="[('is_active', '=', True)]"/>
                <filter name="sht_hr_training_course_filter_inactive" string="Inactive" domain="[('is_active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="sht_hr_training_course_action" model="ir.actions.act_window">
        <field name="name">Courses</field>
        <field name="res_model">sht.hr.training.course</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="sht_hr_training_course_view_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create a training course for your catalog.</p>
        </field>
    </record>
</odoo>
