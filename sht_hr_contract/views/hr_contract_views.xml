<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sht_hr_contract_hr_contract_view_form" model="ir.ui.view">
        <field name="name">hr.contract.form.sht.vietnam</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form"/>
        <field name="arch" type="xml">
            <!-- Hide both occurrences of contract_type_id (Odoo renders two: per-group variant) -->
            <xpath expr="//group[@name='top_info_right']/field[@name='contract_type_id'][not(@groups='hr_contract.group_hr_contract_manager')]" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//group[@name='top_info_right']/field[@name='contract_type_id'][@groups='hr_contract.group_hr_contract_manager']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Thời hạn hợp đồng" name="sht_vietnam_contract" groups="hr.group_hr_user">
                    <group string="Thông tin hợp đồng">
                        <group>
                            <field name="sht_contract_type_id"/>
                            <field name="is_probation" readonly="1"/>
                            <field name="probation_end_date"/>
                            <field name="renewal_count"/>
                            <field name="original_start_date"/>
                        </group>
                        <group>
                            <!-- Rows 44/45/46: show meaningful expiry status -->
                            <field name="contract_expiry_status" readonly="1" widget="badge"
                                   decoration-success="contract_expiry_status == 'ok'"
                                   decoration-warning="contract_expiry_status == 'expiring_soon'"
                                   decoration-danger="contract_expiry_status == 'overdue'"
                                   decoration-muted="contract_expiry_status == 'no_end'"/>
                            <field name="days_to_expire" readonly="1"
                                   invisible="contract_expiry_status not in ('ok', 'expiring_soon')"
                                   string="Số ngày còn lại"/>
                            <field name="days_overdue" readonly="1"
                                   invisible="contract_expiry_status != 'overdue'"
                                   string="Số ngày quá hạn"/>
                            <field name="is_expiring_soon" readonly="1" invisible="1"/>
                        </group>
                    </group>
                </page>
                <page string="Chấm dứt HĐ" name="sht_termination" invisible="state != 'cancel'" groups="hr.group_hr_user">
                    <group>
                        <field name="termination_reason"/>
                        <field name="termination_date"/>
                        <field name="termination_note" nolabel="1" placeholder="Ghi chú..."/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
    <record id="sht_hr_contract_hr_contract_view_list" model="ir.ui.view">
        <field name="name">hr.contract.list.sht.vietnam</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='contract_type_id']" position="after">
                <field name="sht_contract_type_id" optional="show" groups="hr.group_hr_user"/>
                <field name="days_to_expire" optional="show" groups="hr.group_hr_user"/>
            </xpath>
        </field>
    </record>
    <record id="sht_hr_contract_hr_contract_view_search" model="ir.ui.view">
        <field name="name">hr.contract.search.sht.vietnam</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_search"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='running']" position="after">
                <filter string="Sắp hết hạn" name="sht_expiring_soon" domain="[('is_expiring_soon', '=', True)]" groups="hr.group_hr_user"/>
            </xpath>
            <xpath expr="//group[@expand='0']" position="inside">
                <filter string="Loại hợp đồng VN" name="sht_group_contract_type" domain="[]" context="{'group_by': 'sht_contract_type_id'}" groups="hr.group_hr_user"/>
            </xpath>
        </field>
    </record>
</odoo>
